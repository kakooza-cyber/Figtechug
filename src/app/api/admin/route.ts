import { NextRequest } from 'next/server'; import { prisma } from '@/lib/prisma'; import { currentUser, json } from '@/lib/security';
export async function GET(req:NextRequest){const user=await currentUser(req); if(user?.role!=='ADMIN')return json({error:'Forbidden'},403); const [users,deposits,withdrawals,products,pendingDepositRows,pendingWithdrawalRows]=await Promise.all([prisma.user.count(),prisma.deposit.count({where:{status:'PENDING'}}),prisma.withdrawal.count({where:{status:'PENDING'}}),prisma.investmentProduct.count(),prisma.deposit.findMany({where:{status:'PENDING'},include:{user:{select:{phone:true}}},orderBy:{createdAt:'desc'}}),prisma.withdrawal.findMany({where:{status:'PENDING'},include:{user:{select:{phone:true}}},orderBy:{createdAt:'desc'}})]); return json({users,pendingDeposits:deposits,pendingWithdrawals:withdrawals,products,pendingDepositRows,pendingWithdrawalRows})}
