import bcrypt from 'bcryptjs'; import { z } from 'zod'; import { prisma } from '@/lib/prisma'; import { json, phoneSchema, signToken } from '@/lib/security'; import { referralCode } from '@/lib/domain';
const schema=z.object({phone:phoneSchema,password:z.string().min(8),confirmPassword:z.string(),referralCode:z.string().optional()}).refine(d=>d.password===d.confirmPassword,{message:'Passwords must match'});
export async function POST(req:Request){const body=schema.parse(await req.json()); const ref=body.referralCode?await prisma.user.findUnique({where:{referralCode:body.referralCode}}):null; const user=await prisma.user.create({data:{phone:body.phone,passwordHash:await bcrypt.hash(body.password,12),referralCode:referralCode(),referredById:ref?.id,wallet:{create:{}}}}); return json({token:await signToken({sub:user.id,role:user.role}),user:{id:user.id,phone:user.phone,referralCode:user.referralCode}},201)}
