import { NextRequest } from 'next/server'; import { prisma } from '@/lib/prisma'; import { currentUser, json } from '@/lib/security';
export async function GET(req:NextRequest){const user=await currentUser(req); if(!user)return json({error:'Unauthorized'},401); return json(await prisma.deposit.findMany({where:{userId:user.sub},orderBy:{createdAt:'desc'}}))} export async function POST(req:NextRequest){const user=await currentUser(req); if(!user)return json({error:'Unauthorized'},401); const b=await req.json(); return json(await prisma.deposit.create({data:{userId:user.sub,amount:b.amount,senderPhone:b.senderPhone,transactionId:b.transactionId,network:b.network,screenshotUrl:b.screenshotUrl}}),201)}
